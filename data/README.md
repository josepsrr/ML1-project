# Data

The raw dataset is intentionally excluded from version control because the CSV is large.

Run:

```bash
python project.py
```

This will download the Zenodo file to `data/raw/dataset.csv` if it is missing, then run the full analysis.
